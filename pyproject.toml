[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openpi-robot-runtime"
version = "0.1.0"
description = "Mock-first closed-loop runtime and action-adapter layer for remote OpenPI policies."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/openpi_robot_runtime"]

